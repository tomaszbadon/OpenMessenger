import { Contact } from "../datamodel/Contact";
import './Conversation.sass'

export const ConversationComponent: React.FunctionComponent<Contact> = (props) => {

    const { firstName, lastName, avatar } = props

    return <div>

    </div>
}